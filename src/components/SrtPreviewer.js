import React, { Component } from 'react';

export default class SrtPreviewer extends Component {
  constructor(props){
    super(props);
  }

  render(){
    return (
      <div>
        srt previewer
      </div>
    );
  }
}
